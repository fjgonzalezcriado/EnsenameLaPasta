# HV-012: Eliminar simulación — feed 100% real (panel solo-visor)

## Información General
- **Código**: HV-012
- **Fecha inicio**: 2026-06-16
- **Estado**: ✅ Completado
- **Prioridad**: 🟠 Media
- **Tipo**: Evolutivo (Fase 2) / Refactor
- **Depende de**: HV-011 ✅

## Objetivo
Retirar la simulación (RandomWalk) ahora que hay datos reales: el feed en vivo pasa a ser 100% Yahoo y el panel se convierte en **visor** de precios reales (sin auto-trading).

## Decisiones (usuario)
- Borrar RandomWalk por completo (solo datos reales; requiere Internet).
- Desactivar la estrategia automática MA Crossover → panel solo-visor.
- Quitar AAPL/GOOG/BTCUSD del seed (solo HY9H.F).

## Especificaciones
- DI: `IMarketDataProvider` = `YahooFinanceProvider` siempre (se elimina el switch `RegisterMarketDataProvider`). **No** se registra `StrategyExecutionService` (auto-trading off). Código de estrategia/`OrderService` conservado por si se reactiva.
- Borrados: `RandomWalkTickGenerator.cs` + `RandomWalkTickGeneratorTests.cs`.
- `MarketDataOptions` reducido a `ProviderType` (def. "YahooFinance") + `TickIntervalMs` (def. 30000). Eliminados `Symbols`, `Volatility`, `Drift`, `VolumeMin/Max`, `Seed`.
- `appsettings.json`: `MarketData` = ProviderType YahooFinance + TickIntervalMs 30000 + subsección YahooFinance; sin `Symbols` (viven en `TrackedSymbol`).
- Intervalo de sondeo subido a 30 s (rate limits Yahoo).

## Criterios de aceptación
- [x] Provider real único; RandomWalk eliminado
- [x] Estrategia desactivada (sin auto-trading)
- [x] `MarketDataOptions`/`appsettings` limpios
- [x] `dotnet build` + `dotnet test` verdes (96/96)
- [x] Smoke real: arranque OK, `provider=YahooFinance`, tick real de HY9H.F=1390€ persistido

---

## ✅ CIERRE DEL EVOLUTIVO
- **Estado**: ✅ Completado — 2026-06-16 (sesión única)
- **Resultado**: ✅ Cumplido

### Notas / implicaciones
- Sin Internet no hay datos en vivo (era esperado y aceptado).
- El panel mantiene las tarjetas de portfolio/trades, que quedan a 0 al no haber auto-trading. Pendiente opcional: simplificar el dashboard a modo visor puro y/o añadir P&L de holding real (posición vs precio de entrada, p.ej. HY9H.F a 1360€).
- Reactivar estrategia = volver a registrar `StrategyExecutionService` + `IStrategy`/`StrategyOptions` en DI.
